language: python
python:
  - 2.7
install:
  - python bootstrap.py
  - bin/buildout -c buildout.d/travis.cfg

script:
  - ./pre-commit-check.sh
 
notifications:
  irc:
    - "irc.freenode.org#ngep"
  email:
    - it@eestec.net
